const LogoutButton = () => {
  return (
    <div
      onClick={() => {}}
      className="flex items-center font-semibold text-gray-700 hover:bg-gray-400/30"
    >
      로그아웃
    </div>
  );
};

export default LogoutButton;
